export default `
The controlled inputs update rendering when each character input by user.<br/>
For the performance of UI inputs you can use <b>debounce</b> stategy for rendering started after some time.
`