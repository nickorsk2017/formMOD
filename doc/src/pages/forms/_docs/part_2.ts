export default `
The controlled controls update rendering when each character input by user.<br/>
For the performance of UI controls you can use <b>debounce</b> stategy for rendering started after some time.
`