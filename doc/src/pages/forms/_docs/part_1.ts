export default `
<h1 content-header class="content-header">Forms</h1>
You can use 3 types of form:<br/>
<ul content-ul>
    <li>
        <b>1. Form with contolled UI inputs </b> - best if you use UI libraries, but it has less performance for large form.</br>
    </li>
    <li>
        <b>2. Form with uncontrolled UI inputs</b> - maximum performance. Best if you use custom UI or your UI library.</br>
    </li>
    <li>
        <b>3. Form with contolled and uncontrolled UI inputs</b> - also you can use both types inputs inside a form component.</br>
    </li>
</ul>
`