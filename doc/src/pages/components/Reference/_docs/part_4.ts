export default `
    1. Here you got the refMod object which is link of input with all API methods.<br/>
    2. Set <b>ref={refMod.ref}</b> in an input element for linking it with the form model.<br/>
    3. Use <b>refMod.getError()</b> and other API from refMod inside your input component.
`