export default `
<div class="steps">
    <div class="steps-header">Steps</div>
    Step 1: Use setValues method (<b>line 18</b>).<br/>
    Step 2: Define a init value if need(<b>line 23-26</b>).<br/><br/>
    <b>setValues(formValue, {init: true})</b> - init property update form view on fly only first time (on first rendering).<br/>
    It working without react hooks and filled before first render form<br/>
</div>
`