export default `
<div class="steps">
    <div class="steps-header">Steps</div>
    Step 1: Use setValues method (<b>line 21</b>).<br/>
    Step 2: Set a init value if need(<b>line 26-29</b>).<br/>
    <b>setValues(formValue, {skipUpdate: true})</b> - second parameter need to run this method only one time (on first rendering).
</div>
`